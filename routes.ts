/**
 * A route for accessing the detail page for a specific movie
 * This route is used to display detailed information about a movie on its unique identifier
 * @type {string}
 */
export const movieDetailRoute = "/movie";

/**
 * A route for accessing the movies page
 * @type {string}
 */
export const moviesRoute = "/movies";

/**
 * A route for accessing the popular movies page
 * @type {string}
 */
export const popularMoviesRoute = "/movies/popular";

/**
 * A route for accessing the search page
 * This route leads to a page displaying the search results for a given query
 * @type {string}
 */
export const searchRoute = "/search";
